import os
import stardog
from dotenv import load_dotenv

load_dotenv()

conn_details = {
    'endpoint': 'https://sd-22eb3c93.stardog.cloud:5820',
    'username': os.getenv('STARDOG_USERNAME'),
    'password': os.getenv('STARDOG_PASSWORD')
}


query = """
SELECT ?s ?p ?o
FROM <tag:stardog:studio:ibr:model>
WHERE {
  ?s ?p ?o
}
LIMIT 5
"""

# Connect to "defect" database for queries/updates
with stardog.Connection('defect', **conn_details) as conn:
    # Example: do a SPARQL SELECT
    results = conn.select(query)
    print(results)
